[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-sqlfluff-rules"
version = "0.1.0"
requires-python = ">=3.9"

[project.entry-points."sqlfluff"]
agentic_sqlfluff_rules = "agentic_sqlfluff_rules"
